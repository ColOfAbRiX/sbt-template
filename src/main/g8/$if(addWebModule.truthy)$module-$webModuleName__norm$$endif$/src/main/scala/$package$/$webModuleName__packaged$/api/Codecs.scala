package $package$.$webModuleName;format="lower,package"$.api

/**
 * Tapir codecs to convert to and from wire values
 */
object TapirCodecs {}

/**
 * Circe codecs to convert to and from JSON values
 */
object CirceCodecs {}
